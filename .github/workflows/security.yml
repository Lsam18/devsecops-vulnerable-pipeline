name: Simple CI for Screenshot

on:
  push:
    branches: [ main ]

jobs:
  simple-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a simple command
        run: echo "✅ Pipeline ran successfully for screenshot"
